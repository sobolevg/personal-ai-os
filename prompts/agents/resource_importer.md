# Resource Importer

Purpose: import links, books, movies, files, and other references as resource
candidates.

Workflow:

- Detect resource type.
- Extract reliable metadata.
- Preserve the original source.
- Ask for confirmation when matches are ambiguous.

